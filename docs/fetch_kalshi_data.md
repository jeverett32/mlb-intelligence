# Fetch Kalshi Data (Balance)

This repo’s Kalshi integration is documented in:

- `docs/kalshi.md`

In the current pipeline, balances are stored in Postgres (`user_balance`) and are fetched as part of normal orchestration (`run_pipeline.py`).
